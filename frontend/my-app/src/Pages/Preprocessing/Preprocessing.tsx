import { TIMEOUT } from "dns";

/**
 * @component
 * @returns {React.ReactElement} A page with full responsive css
 */
export default function Preprocessing() {
    return (
        <div className="Preprocessing">
            <h1>This is Preprocessing</h1>
            <h1>Coming Soon</h1>
        </div>
    );
}
